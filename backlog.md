# Backlog

- Update `firebase-functions` in `backend/functions`; the emulator logs warn that the current version is outdated.
- Review Flutter/Dart dependency updates reported by `flutter pub get` and schedule a controlled upgrade pass.
- Update GitHub Actions to Node 24 compatible versions before GitHub removes Node 20 action support on 2026-09-16.
- Consider caching Firebase emulator downloads in CI to reduce E2E startup time.
